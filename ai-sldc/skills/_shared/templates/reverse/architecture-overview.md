# 架构概览

> 此文档由 ai-sldc-sync 自动生成，请根据实际情况微调

## 基本信息

| 属性 | 值 |
|------|-----|
| 项目名称 | [项目名] |
| 技术栈 | [从依赖配置提取] |
| 代码规模 | [文件数 / 代码行数] |
| 生成时间 | YYYY-MM-DD HH:mm |

---

## 目录结构

```
[项目根目录]/
├── src/
│   ├── api/           # API 接口层
│   ├── services/      # 业务逻辑层
│   ├── models/        # 数据模型层
│   ├── utils/         # 工具函数
│   └── config/        # 配置文件
├── tests/             # 测试文件
├── docs/              # 文档
└── [配置文件]
```

---

## 模块划分

| 模块 | 路径 | 职责 | 主要文件 |
|------|------|------|---------|
| API层 | src/api/ | 接口定义、请求处理 | [文件列表] |
| 业务层 | src/services/ | 业务逻辑处理 | [文件列表] |
| 数据层 | src/models/ | 数据模型定义 | [文件列表] |
| 工具层 | src/utils/ | 通用工具函数 | [文件列表] |

---

## 依赖关系

```
┌─────────────────────────────────────────┐
│                  API层                   │
│    [handler 文件列表]                    │
└─────────────────┬───────────────────────┘
                  │ 调用
                  ↓
┌─────────────────────────────────────────┐
│                 业务层                   │
│    [service 文件列表]                    │
└─────────────────┬───────────────────────┘
                  │ 调用
                  ↓
┌─────────────────────────────────────────┐
│                 数据层                   │
│    [model 文件列表]                      │
└─────────────────────────────────────────┘
```

---

## 技术栈详情

### 运行时依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| [依赖名] | [版本] | [用途说明] |

### 开发依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| [依赖名] | [版本] | [用途说明] |

---

## 技术决策

### 框架选择

- **Web框架**: [框架名] - [原因]
- **ORM**: [ORM名] - [原因]
- **数据库**: [数据库类型] - [原因]

### 架构模式

- **架构风格**: [MVC / 分层 / 微服务 / ...]
- **状态管理**: [方案]
- **认证方式**: [方案]

---

## 关键文件

| 文件 | 用途 | 重要性 |
|------|------|--------|
| [入口文件] | 应用入口 | ★★★★★ |
| [配置文件] | 环境配置 | ★★★★☆ |
| [路由文件] | 路由定义 | ★★★★☆ |

---

## 开发约定

### 命名规范

- 文件命名: [规范]
- 变量命名: [规范]
- 函数命名: [规范]

### 目录规范

- [规范说明]

---

## 追溯信息

```yaml
generated_by: ai-sldc-sync
source_commit: [git commit hash]
analysis_scope: [分析的文件范围]
```
